Specifying delimiter for Excel 2007 export
------------------------------------------
1.Bring up your default options window by clicking Start -> Settings -> Control Panel -> Regional Settings.
2.Click the "Customize" button.
3.Click the “Number” tab and in the “List Separator” field, replace the current default separator with the one you want to use (in your case, the pipe symbol).
4.Click “OK” to save the change and close the window.

How to view chrome saved passwords??
-------------------------------------
chorme://settings/passwords

How to uninstall Visual Studio 2015??
Find the mount iso, or vs_ultimate.exe
shift + Right Click, OPEN prompt
D:\vs_community.exe /uninstall /force 
wmic product where "name like 'microsoft visual%'" call uninstall /nointeractive

---
How to force a blue screen in windows?
http://www.wikihow.com/Force-a-Blue-Screen-in-Windows



Batch scripting
--------------------
http://ss64.com/nt/
1. What does the %~dp0 mean??
The driver letter and path of that batch file.

2. batch script kill processes?
taskkill /f /im EXCEL*


VC .NET 2003中
std::vector<float *> vecEmpty;
sizeof(vecEmpty)=16
std::_Vector_val<>
_Myfirst
_Myend
_Mylast

C#
1. Changing the value of a DataRow.ItemArray does not work
The ItemArray property creates a new array and copies the values from the row in that array. Changing it has no impact on the row.
foreach (DataRow dr in dtBilder.Rows)
if (dr[0].Equals(b.ID))
{
  dr[1] = b.Name;                 // string
  dr[2] = b.Beschreibung;         // string - probably null
  dr[3] = b.QString;              // string
  dr[4] = b.InformationenGeladen; // bool
  dr[5] = b.Hash;                 // int
  dr[6] = b.Hoehe;                // int
  dr[7] = b.Breite;               // int
  dr[8] = b.AuflX;                // float
  dr[9] = b.AuflY;                // float
  dr[10] = b.MD5;                 // byte[] - currently null
  break;
}

2.	system.IO.FileNotFoundException: cannot find assembly or file Mysql.Data
You added Mysql.Data in your project's references folder, if I understood well.
Now, right click on this in VS Explorer, go to properties, and set Attribute "Copy Local" to true.
So it will be copied in the "debug" or "release" folder whenever you build your app.

3. system.runtime.interopservices.comexception (0x800a03ec) 无法访问文件。请尝试下列方法之一:
确认所指令的文件夹已存在
确认文件所在的文件夹不是只读的
确认给出的文件名不包含下列字符
确认文件/路径名长度不超过218个字符
	解决方法：杀掉任务管理器，里面所有的EXCEL.EXE进程.就可以了。
	(tips:遇到无法访问文件，先看下文件是否被另一个进程占用,或者文件的路径名,写文件的权限够不够)
	
	网上有说windows server 上的解决办法：
	The solution for this appalling BUG in Microsoft IIS & Excel is terrific:
Create directory "C:\Windows\SysWOW64\config\systemprofile\Desktop " (for 64 bit Windows) or "C:\Windows\System32\config\systemprofile\Desktop " (for 32 bit Windows)
Set Full control permissions for directory Desktop (for example in Win7 & IIS 7 & DefaultAppPool set permissions for user 
"IIS AppPool\DefaultAppPool")

4.	修改MTU
	netsh interface ipv4 show subinterfaces
	netsh interface ipv4 set subinterface "VPN连接名称" mtu=1280 store=persistent
	
5. Set up Python on windows to not type python in cmd
   C:\> assoc .py=Python
   C:\> ftype Python="C:\python27\python.exe %1 %*"
   similar to how it works for .exe files, add .py to the semi-colon separated list of the (global) PATHEXT variable.
   echo %PATHEXT%
   
6. 华硕主板P8B75-V 开启 AHCI（Serial ATA Advanced Host Controller Interface）串行ATA高级主控接口/高级主机控制器接口
   1. regedit 打开注册表编辑器,  找到 HKEY_LOCAL_MACHINE\SYSTEM\CurrentControlSet\services\msahci 双击右边的 Start 将其数值改为0
   2. 重启电脑,按 DEL 进入 BIOS, 再进入 Advanced Mode, 选择 SATA configuration 将 IDE 改为 AHCI
   3. 改完后进入系统, 系统会自动安装驱动,安装完后会问你是否立即重启,重启完成后就开启了AHCI
   
7. How to migrate system to SSD?
   1. http://www.samsung.com/global/business/semiconductor/minisite/SSD/global/html/support/downloads.html
   download SDM (Samsung data migration software)
   2. install SDM and open it, then do it.
   
8. 如何查看 4K对齐?
   WIN + R 
   msinfo32
   组件 -> 存储 -> 磁盘, 用 分区起始偏移 / 4 * 1024 = 如果是整数，表明4K 对齐
   
9. 查看windows激活状态
   cmd 
   slmgr /dlv
   
10. wireshark how to capture traffic for a specific program ?
    netstat -b, find the program
    127.0.0.1:xxxxx, where xxxxx is the local port number for the connection.
    in Wireshark, tcp.port == xxxxx

batch file tips
1.comment one line :
@rem this is the commet line

Commenting multiple lines (block comments) in batch file
goto comment
...skip this
:comment
(Notepad++, select the code blocks, CTRL + Q)
2.Delete files in a directory recursively
rd /s /q /path/of_directory
md /path/of_directory

3.How to copy recursively??
mkdir dstFolder
xcopy srcFolder dstFolder /s /e /y

4.How to append A file to B file??
type A >> B
5.How to append text to file named FILE??
echo %textVariable% >> FILE
6.more than one remote source not supported
路径名中带空格,需要加引号,将路径引起来
7.How to set a variable with the result of a command??
for /f "delims=" %%a in ('command') do @set theValue=%%a
8.Batch file get current date as yyyymmdd?? get time stamp(http://stackoverflow.com/questions/203090/how-to-get-current-datetime-on-windows-command-line-in-a-suitable-format-for-us)
@echo off
For /f "tokens=2-4 delims=/ " %%a in ('date /t') do (set mydate=%%c-%%a-%%b)
For /f "tokens=1-2 delims=/:" %%a in ('time /t') do (set mytime=%%a%%b)
echo %mydate%_%mytime%


FOR /F “TOKENS=1* DELIMS= ” %%A IN (‘DATE/T’) DO SET CDATE=%%A
FOR /F “TOKENS=1,2 eol=/ DELIMS=/ ” %%A IN (‘DATE/T’) DO SET mm=%%B
FOR /F “TOKENS=1,2,3 DELIMS=/ eol=/” %%A IN (‘echo %CDATE%’) DO SET dd=%%C
FOR /F “TOKENS=1,2,3 DELIMS=/ ” %%A IN (‘echo %CDATE%’) DO SET yyyy=%%A
SET date=%mm%%dd%%yyyy%


9. Set a variable with space?
	set "tempVar= ohohoh"
	echo %tempVar%
	
10.	pscp.exe basics
	http://the.earth.li/~sgtatham/putty/0.60/htmldoc/Chapter5.html
	To receive (a) file(s) from a remote server:
	pscp [options] [user@]host:source target
	
	To send (a) file(s) to a remote server:
	pscp [options] source [source...] [user@]host:target
	
11.	How to make the pscp  execute in batch script without break?? plink first time
	Add SSH host key to another user's PuTTY cache
	pscp the server's host key is not cached in the registry 
	
	echo y | pscp -i /path/to/key/file user@remote:/tmp/file  .
	echo y | plink -i /path/to/key/file scripts.sh
	
12.	securecrt the folder is not valid
	将文件名或者目录名 改短一些就可以了

13.	SecureCRT 将标签栏 设置为 不包含 路径
	选项 -> 全局设置 -> General -> Default Session -> Edit Default Settings -> terminal -> emulation -> advanced -> ignore window title change requests即可，这样session名字就永远不会变了。
	
14. 如何读取到Chrome的历史记录？
   C:\Users\UserName\AppData\Local\Google\Chrome\User Data\Default (on Windows 7).
   Install ODBC server for sqllite3.
   
15. How to copy contents of cmd prompt ? 如何拷贝cmd窗口里面的内容
Open Command Prompt
Right-click the title bar of the command prompt window, point to Edit, and then click Mark.
Click the beginning of the text you want to copy.
Press and hold down the SHIFT key, and then click the end of the text you want to copy (or you can click and drag the cursor to select the text).
Right-click the title bar, point to Edit, and then click Copy.

16. batch script arguments (http://ss64.com/nt/syntax.html)
  %* for everything
  %0 - the command used to call the batch file (could be foo, ..\foo, c:\bats\foo, etc.)
  %1 is the first command line parameter,
  %2 is the second command line parameter,
  and so on till %9 (and SHIFT can be used for those after the 9th).
  %~nx0 - the actual name of the batch file, regardless of calling method (some-batch.bat)
  %~dp0 - drive and path to the script (d:\scripts)
  %~dpnx0 - is the fully qualified path name of the script (d:\scripts\some-batch.bat)
  
17.  build VS2012 VS2013 project using msbuild
  1.Find the position of VS2012 x86 native tools command prompt(start menu, right click)
  2.copy to the project.sln file directory 
  3.double click, and type "msbuild project.sln"
  
18. svn update error : 
    sqllite: database disk image is malformed
    sqlite3 .svn/wc.db "reindex nodes"
    sqlite3 .svn/wc.db "reindex pristine"
    
    sqlite common commands
    (http://www.sqlite.org/cli.html)
    (http://zetcode.com/db/sqlitepythontutorial/)
    (http://sebastianraschka.com/Articles/2014_sqlite_in_python_tutorial.html)
    .help
    .exit
    .tables
    .schema
    .schema tableName
    create table tbl1(col1 INT, col2 INT, col3 INT)
    insert into tableName values(1,2,strftime('%s', '2015-05-20 11:00:00'))
    .mode list
    .separator ","
    .width 12 6
    .output all_results.txt
    left join:
    SELECT table1.*, table2.column as my_column_name_no_in_table1 FROM table1 LEFT JOIN table2 ON table1.column = table2.column
    
19. find where did incredibuild "MakeDirsForLink" come from?
    1. google, find MakeDir Task, https://msdn.microsoft.com/en-us/library/s2448zz7(v=vs.110).aspx
    2. search the .vcxproj file, did not find MakeDir Task configuration
    3. open x86 native tools command prompt, run msbuild /v:d proj.sln > temp
    4. search "MakeDirsForLink" the file named temp, find 3 positions, 来自项目 aaa.vcxproj 的文件 "C:\Program Files (x86)\MSBuild\Microsoft.Cpp\v4.0\v110\Microsoft.CppBuild.targets"中的目标"MakeDirsForLink"(目标"_Link"依赖于它):
    任务 MakeDir
       Target "MakeDirsForLink: (TargetId:160)" in file "C:\\Program Files (x86)\\MSBuild\\Microsoft.Cpp\\v4.0\\Microsoft.CppBuild.targets" from project "C:\\Builds\\17\\RoLAN\\IntelTest\\Sources\\projects\\rolansource\\RoLAN\\IntelProcessing\\Empty\\Empty.vcxproj" (target "_Link" depends on it):
    5. Open C:\Program Files (x86)\MSBuild\Microsoft.Cpp\v4.0\v110\Microsoft.CppBuild.targets
    6. find MakeDirsForLink,  <PropertyGroup><_LinkImportLibrary>%(Link.ImportLibrary)</_LinkImportLibrary></PropertyGroup>
       right click project, properties, Linker,  advance, import library, set it to null.
    (Problem solved.)
    
20. Link : fatal error LNK1104, 无法打开文件 "..\..\aaa.lib"
    1. search .vcxproj file, did not find aaa.lib 
    2. msbuild /p:Configuration=Release /v:d .sln > temp, still did not give a clue
    3. search "aaa.lib" in source code, find #pragma comment(lib, "..\..\aaa.lib"), according to MSDN, https://msdn.microsoft.com/en-us/library/7f0aews7(v=vs.110).aspx
    #pragma comment( comment-type [,"commentstring"] )
    Places a library-search record in the object file. This comment type must be accompanied by a commentstring parameter containing the name (and possibly the path) of the library that you want the linker to search.
    According to SO, http://stackoverflow.com/questions/12199595/c-what-does-pragma-commentlib-xxx-actually-do-with-xxx
    Mostly, it's used to support different versions: 
    #ifdef USE_FIRST_VERSION
    #pragma comment(lib, "vers1.lib")
    #else
    #pragma comment(lib, "vers2.lib")
    #endif
    When your application uses a dynamically-linked library, a lib file tells you information about what symbols are exported in the dll. So basically you only need the lib to compile & link, but you need the dll to run the program, as it contains all the binary code.
    
21.  github search 
    user:mozilla language:python
    
22. dbghelper dump example(How to write a sample code that will crash and produce dump file?)
http://stackoverflow.com/questions/5028781/how-to-write-a-sample-code-that-will-crash-and-produce-dump-file
#include <Windows.h>
#include <Dbghelp.h>

void make_minidump(EXCEPTION_POINTERS* e)
{
    auto hDbgHelp = LoadLibraryA("dbghelp");
    if(hDbgHelp == nullptr)
        return;
    auto pMiniDumpWriteDump = (decltype(&MiniDumpWriteDump))GetProcAddress(hDbgHelp, "MiniDumpWriteDump");
    if(pMiniDumpWriteDump == nullptr)
        return;

    char name[MAX_PATH];
    {
        auto nameEnd = name + GetModuleFileNameA(GetModuleHandleA(0), name, MAX_PATH);
        SYSTEMTIME t;
        GetSystemTime(&t);
        wsprintfA(nameEnd - strlen(".exe"),
            "_%4d%02d%02d_%02d%02d%02d.dmp",
            t.wYear, t.wMonth, t.wDay, t.wHour, t.wMinute, t.wSecond);
    }

    auto hFile = CreateFileA(name, GENERIC_WRITE, FILE_SHARE_READ, 0, CREATE_ALWAYS, FILE_ATTRIBUTE_NORMAL, 0);
    if(hFile == INVALID_HANDLE_VALUE)
        return;

    MINIDUMP_EXCEPTION_INFORMATION exceptionInfo;
    exceptionInfo.ThreadId = GetCurrentThreadId();
    exceptionInfo.ExceptionPointers = e;
    exceptionInfo.ClientPointers = FALSE;

    auto dumped = pMiniDumpWriteDump(
        GetCurrentProcess(),
        GetCurrentProcessId(),
        hFile,
        MINIDUMP_TYPE(MiniDumpWithIndirectlyReferencedMemory | MiniDumpScanMemory),
        e ? &exceptionInfo : nullptr,
        nullptr,
        nullptr);

    CloseHandle(hFile);

    return;
}

LONG CALLBACK unhandled_handler(EXCEPTION_POINTERS* e)
{
    make_minidump(e);
    return EXCEPTION_CONTINUE_SEARCH;
}

int main()
{
    SetUnhandledExceptionFilter(unhandled_handler);

    return *(int*)0;
}

23. How to break on access violation handler code??
前能想到的办法就是封装一个类,然后再代码中调用该类的方法.
windbg -y -i ..\exe -v -z gameserver_20160529_084601.dmp

24. fprintf flush
fprintf(m_fPointer, "output string to file");
fflush(m_fPointer);

25. How to use windbg to debug dmp files??
http://www.debuginfo.com/articles/effminidumps.html

26. How to make VS break only on unhandled exceptions?
Make sure you have Just My Code Enabled by going into Tools-->Options-->Debugger-->General--> Enable Just My Code.
This will change your Debug--> Exceptions Dialog Box to show a CheckBox for User-unhandled Errors.

27. How to break on handled exceptions?
常用模式是 在exception 地方弹出一个messageBox,让进程锁住,然后用 vs 附加到该进程.

28. How to Catching access violation exceptions?
http://stackoverflow.com/questions/457577/catching-access-violation-exceptions


29. How to debug exe without source code?
File --> Open Project/Solution, Choose exe file. Press F5.

中英文术语对照表
空格 whitespace


svn 删除错误提交
http://blog.csdn.net/netwalk/article/details/14122419

=================
LNK1000: Internal error during IMAGE::BuildImage
It began work as soon as I have changed "Whole program optimization" option from "Use Link Time Code Generation" to "Profile Guided Optimization-Instrument".
“Whole program optimization” VS .ENT 2003,Open the project's Property Pages,Configuration Properties, General property,Whole Program Optimization 
将选项设置为 开

==================
小番茄破解目录(http://velep.com/archives/1055.html)
C:\Users\username\AppData\Local\Microsoft\VisualStudio\12.0\Extensions\o2xc5z32.hww

===================
gcc -std=gnu99 -O2 -Wall -Wextra -DLUA_COMPAT_5_2 -DLUA_USE_LINUX    -c -o lua.o lua.c
lua.c:80:31: fatal error: readline/readline.h: No such file or directory
 #include <readline/readline.h>
sudo apt-get install libreadline-dev 

===
How to produce high CPU load on Windows?(http://superuser.com/questions/396501/how-can-i-produce-high-cpu-load-on-windows)
consume.exe from Windows Server 2003 Resource Toolkit.
C:\Program Files\Microsoft SDKs\Windows\v7.1\Bin\x64>consume -cpu-time -time 5

How to produce high CPU load on Linux?
cat /dev/zero > /dev/null
sudo apt-get install stress
./stress --cpu 3
from multiprocessing import Pool

def f(x):
    # Put any cpu (only) consuming operation here. I have given 1 below -
    while True:
        x * x

# decide how many cpus you need to load with.
no_of_cpu_to_be_consumed = 3

p = Pool(processes=no_of_cpu_to_be_consumed)
p.map(f, range(no_of_cpu_to_be_consumed))

====================
why public virtual deconstructor??
what is the structure of static template classes ?  What does the compiler do??

====================
How to eliminate certain annoying message during framework source code debugging
1.Open "Region" from Control Panel
2.Choose "Administrative" tab
3.Check if "Language for non-Unicode programs" is any UTF-8 or Unicode language that other than English (In my case it is Chinese). If so, click "Change system locale..." to change it to English.
4.Reboot

====================
Use 7z to pack .bz2 file
7z a -ttar test.tar source\* -r
7z a -tbzip2 test.tar.bz2 test.tar



===================
the name of the thread
the thread id of the thread
Runnable function of the thread
mutex lock of the thread
Condition variable lock of the the thread

======================
undeclared identifier 'NULL'
#include <stddef.h>

======================
undefined reference to `pthread_create'
use -lpthread as the last compile flag

========================
convert tabs to spaces
find ./ -type f -not -iwholename '*.git*' -exec sed -i 's/\t/####/g' {} \;


========================
template multiple definition of
将构造函数申明放在.h文件
实现一定要放在.cpp文件里面,否则会报错s
g++ -g test_client.cpp BaseTCPClient.cpp BaseSocket.cpp BaseThread.cpp BaseByteBuffer.cpp  -lpthread -o test_client
g++ -g test_client.cpp BaseTCPClient.cpp BaseSocket.cpp BaseThread.cpp BaseByteBuffer.cpp  -lpthread -o test_client

=========================
visual studio auto refresh file changes
Options --> Environment --> Documents --> Detect when file is changed outside the document, auto-load changes, if saved

visual studio include linux headers?
tar -jcf all_linux_include.tar.bz2 /usr/include/
copy all_linux_include.tar.bz2 to windows machine
new folder linux under C:\Program Files (x86)\Microsoft Visual Studio 14.0\VC\crt\src
Visual studio , Properties, C++, General, Additional Include Directiories, add C:\Program Files (x86)\Microsoft Visual Studio 14.0\VC\crt\src


visual studio disable all assert in debug mode?(http://stackoverflow.com/questions/28041359/how-to-turn-off-asserts-in-debug-mode-in-visual-studio-2013)
_set_error_mode(_OUT_TO_STDERR);
int iPrev = _CrtSetReportMode(_CRT_ASSERT,0);
// Start Operation with no ASSERTs
...
// Restore previous mode with ASSERTs
_CrtSetReportMode(_CRT_ASSERT,iPrev);
#undef assert
#define assert(x) ((void)0)

========================
make errors(vim Makefile :%s/^[ ]\+/^I/)
NUL character seen; rest of line ignored
Q: When I invoke Make, it refuses to do anything and prints a cryptic message: "makefile:10: *** missing separator. Stop." Now what kind of excuse is that? 
A: Unlike most other DOS Make programs which accept any whitespace character at the beginning of a command in a rule, GNU Make insists that every such line begins with a TAB. (Most other Unix Make programs also require TABs, and the Posix standard requires it as well.) Make sure that the line whose number is printed in the error message (in this case, line 10) begins with a TAB.

Beginning with version 3.78, GNU Make prints a message that hints at a possible SPACEs-vs-TAB problem, like this:
*** missing separator (did you mean TAB instead of 8 spaces?).  Stop.

link errors:
Error	LNK2038	mismatch detected for '_MSC_VER': value '1700' doesn't match value '1900' in RaycastCallbackDemo.obj hkBase.lib
1. set _MSC_VER 
  hkBase.lib is built with VS2012(1700)
  RaycastCallbackDemo.obj is built with VS2015(1900)
  Predefined libraries(https://msdn.microsoft.com/en-us/library/b0084kay.aspx)
  For example, if the version number of the Visual C++ compiler is 17.00.51106.1, the _MSC_VER macro evaluates to 1700. Type cl /? at the command line to view the compiler's version number.
2. project, right click, Properties
  Properties > Config Properties > General > Platform Toolset is  v100 for visual studio 2010, v110 for visual studio 2012.

3. Error	MSB8020	The build tools for Visual Studio 2012 (Platform Toolset = 'v110') cannot be found. To build using the v110 build tools, please install Visual Studio 2012 build tools.  Alternatively, you may upgrade to the current Visual Studio tools by selecting the Project menu or right-click the solution, and then selecting "Retarget solution".
  google 'Microsoft Build Tools 2012'
  
如何不安装VS2012 但能够使用 cl.exe 进行编译?
从 另一台电脑上压缩拷贝 
C:\Program Files (x86)\Microsoft Visual Studio 11.0\VC\ 
C:\Program Files (x86)\Microsoft Visual Studio 11.0\Common7\
检查环境变量里面  VS110COMNTOOLS 的路径是否正确
最后尝试失败，重新安装vs2012

---
set up vps as proxy server
https://www.digitalocean.com/community/tutorials/how-to-set-up-ssh-tunneling-on-a-vps

===
VS 多行注释
CTRL +K, CTRL + C
CTRL + E,CTRL + U,取消注释
编辑 --> 高级 里面可以设置

set breakpoints to all methods in a class at once in VS?
CTRL + B, ClassName::*

===
Game making viedos
游戏名 + Making of / Behind the scenes / Bonus / Feature / Development 
游戏名 +花絮 / 幕后 / 特典 / 访谈



============================
How to build qt5 with visual studio 2013??
(http://stackoverflow.com/questions/15826893/building-qt5-with-visual-studio-2012-visual-studio-2013-and-integrating-with)
1. Install DirectX11 SDK
	1.1 install DirectX10 SDK
		1.1.1  Remove the Visual C++ 2010, 2012, 2013(any version greater than 10.0.40219,所有高版本的都要移除) Redistributable Package version 10.0.40219
				MsiExec.exe /passive /X{F0C3E5D1-1ADE-321E-8167-68EF0DE699A5}
				MsiExec.exe /passive /X{1D8E6291-B0D5-35EC-8441-6616F567A0F7}
				Control Panel -> Uninstall programs -> C++2012
				Restart the computer, 先重启电脑,否则有些进程会占用
		1.1.2  Install the DirectX SDK (June 2010)
		1.1.3  Reinstall the Visual C++ 2010 Redistributable Package version 10.0.40219(KB 2565063)
	1.2 install DirectX11 SDK(Do not choose "Application Verifier", 只安装必要的就行了,别多装,会失败的)
	1.3 install Python for windows	
	1.4 install Perl for windows from ActiveState(http://www.activestate.com/activeperl/downloads)
	1.5 install GitHub for Windows. Add git.exe to system path.
		(记得Perl的环境变量的路径,要在github的前面,否则会报错)
	1.6 (该项可选,可不选)"GetOptionsFromArray" is not exported by the Getopt::Long module Can't continue after import errors at init-repository line 201 BEGIN failed--compilation aborted at init-repository line
		find the git.exe directory, rename perl.exe to perl1.exe(删掉git自带的perl.exe)
	1.7 Uninstall Avast
2.	cd E:\
	git clone https://gitorious.org/qt/qt5.git qt5
	(How to cd another directory vs2013 Command Prompt?)
	Visual studio 2013 tools, VS2013 x86 Native Tools Command Prompt, copy to E:\, right click, Properties, Start Position.
	(VS2013 tools, 选择VS2013 x86 Native Tools Command Prompt, 打开目录,拷贝到E盘,右键,属性,起始位置改为E:\qt5)
	双击VS2013 x86 Native Tools Command Prompt
	
	2.1 perl ./init-repository --no-webkit
	2.2 configure -prefix "e:\qt5\make_product" -developer-build -opensource -confirm-license -mp -nomake examples -nomake tests -debug-and-release -c++11 -no-warnings-are-errors -platform win32-msvc2013.
	(the c++11 option might not be necessary with the VS2012 compiler, but works with VS2013)
	(the -no-warnings-are-errors is necessary in case you get errors on a 64-bit automatic build of ANGLE; -platform is automatically set to win32-msvc2012, so by default the 32-bit build of Qt is used, and -platform probably does not need to be supplied at the command line (EVEN if you have previous versions of VS installed). For VS2013, use -platform win32-msvc2013.)
	2.3 nmake
	2.4 nmake install
	
3.	How to fix errors?
	3.0 控制面板,区域和语言,管理,非Unicode程序的语言,选择"英语(美国)"
		You can do this by Control Panel - Regions - Administrative - Language for non-Unicode programs.
		
		Warning C4819: The file contains a character that cannot be represented in the current code page (932). Save the file in Unicode format to prevent data loss
		When you see this message, you may use Chinese, Japanese or Korean Windows, and uses DBCS code page.
		To avoid this warning, you need to change your Windows system code page to English or another SBCS code page. 
	3.1 (Download VS2013.Update.4.exe, and update VS2013)
	error C2059: syntax error : '<fake-expression>'
        f:\myworkspace\chromium\chromium\src\gin\function_template.h(152) : while compiling class template member function 'gin::internal::Invoker<Indices,>::Invoker(gin::Arguments *,int)'
        f:\myworkspace\chromium\chromium\src\gin\function_template.h(201) : see reference to function template instantiation 'gin::internal::Invoker<Indices,>::Invoker(gin::Arguments *,int)' being compiled
        f:\myworkspace\chromium\chromium\src\gin\function_template.h(201) : see reference to class template instantiation 'gin::internal::Invoker<Indices,>' being compiled
        f:\myworkspace\chromium\chromium\src\gin\function_template.h(190) : while compiling class template member function 'void gin::internal::Dispatcher<Sig>::DispatchToCallback(const v8::FunctionCallbackInfo<v8::Value> &)'
        with
        [
            Sig=void (void)
        ]
        f:\myworkspace\chromium\chromium\src\gin\function_template.h(225) : see reference to function template instantiation 'void gin::internal::Dispatcher<Sig>::DispatchToCallback(const v8::FunctionCallbackInfo<v8::Value> &)' being compiled
        with
        [
            Sig=void (void)
        ]
        f:\myworkspace\chromium\chromium\src\gin\function_template.h(223) : see reference to class template instantiation 'gin::internal::Dispatcher<Sig>' being compiled
        with
        [
            Sig=void (void)
        ]
        f:\myworkspace\chromium\chromium\src\gin\object_template_builder.h(42) : see reference to function template instantiation 'v8::Local<v8::FunctionTemplate> gin::CreateFunctionTemplate<void(void)>(v8::Isolate *,const base::Callback<void (void)>,int)' being compiled
        f:\myworkspace\chromium\chromium\src\gin\object_template_builder.h(41) : while compiling class template member function 'v8::Handle<v8::FunctionTemplate> gin::`anonymous-namespace'::CallbackTraits<T,void>::CreateTemplate(v8::Isolate *,const base::Callback<void (void)> &)'
        with
        [
            T=base::Callback<void (void)>
        ]
        f:\myworkspace\chromium\chromium\src\gin\object_template_builder.h(106) : see reference to function template instantiation 'v8::Handle<v8::FunctionTemplate> gin::`anonymous-namespace'::CallbackTraits<T,void>::CreateTemplate(v8::Isolate *,const base::Callback<void (void)> &)' being compiled
        with
        [
            T=base::Callback<void (void)>
        ]
        f:\myworkspace\chromium\chromium\src\gin\object_template_builder.h(106) : see reference to class template instantiation 'gin::`anonymous-namespace'::CallbackTraits<T,void>' being compiled
        with
        [
            T=base::Callback<void (void)>
        ]
        f:\myworkspace\chromium\chromium\src\gin\modules\timer.cc(37) : see reference to function template instantiation 'gin::ObjectTemplateBuilder &gin::ObjectTemplateBuilder::SetMethod<base::Callback<void (void)>>(const base::StringPiece &,const T &)' being compiled
        with
        [
            T=base::Callback<void (void)>
        ]
	ninja: build stopped: subcommand failed.


服务器最重要的：
thread
socket
thread pool
epoll
---
aws 
1. putty connect EC2
打开puttyGen,点击Load, 选择.pem文件，点击Save Private Key保存.ppk文件
在 Connection --> SSH --> Auth 里面Browse，选择.ppk文件

---
Chrome
Where is cookies data stored??
C:\Users\your_username\AppData\Local\Google\Chrome\User Data\Default\
C:\Users\Your User Name\AppData\Local\Google\Chrome\User Data\Default\Local Storage


---
Enterprise Architect
1. How to set the default class language to C++ ??
2. How to draw association(Uni-directional,bi-directional association) ?
 Choose association, mouse left button down, drag from the source class to the target class , left button up.
3. How to add properties and methods to a class ?
	At "Project Browser" Window, select the class, Right click, Attributes , operations.
4. How to change class to interface ??
   Select the class,
   look at Element Properties View
   The Properties window has three branches: Class Settings (or Interface, etc), Project, and Advanced
   look under Class Settings, there's a field called Type with an ellipsis button
   Be aware that you may need to double-check the element properties once you've changed the type. For instance, changing a simple Class to an Interface is pretty straightforward. If on the other hand you change an Interface to a Class, the class will be abstract (because an Interface is) and retain the "interface" stereotype.
5. basic
   1.	File  New Project, choose directory and filename
	2.	Select UML Structural  Class
	3.	Delete the example classes and diagram
	4.	Drag a class to the canvas
	5.	Right click, add attributes and options to the class
	Email String
	numOfEmail int
	6.	Add a association Relationship : Select the associate in Toolbox’s Class Relationships
	Click on class A, and drag to class B, left button up.
	Right click the line between class A and B, choose Source Role and Target Role
	Choose Direction
	7.	Drag Enumeration class to the canvas

6.  enterprise architect save diagram as image
	CTRL + T
	short keys(http://www.sparxsystems.com/enterprise_architect_user_guide/9.3/getting_started/keyboardshortcuts.html)
	enterprise architect add attribute shortkey
	CTRL + SHIFT + F9
	
7. enterprise architect copy between models(http://www.sparxsystems.com/enterprise_architect_user_guide/9.0/modeling_basics/duplicateadiagram.html)
	Add a package
	Right Click --> Copy Diagram --> go to another package, Right Click --> Paste Diagram --> Smart Paste
	
8. how to draw c++ template class diagram (Enterprise Architect, right click class ,choose templates, add parameters)
           .......               
___________:  T  :
|          :.....:
|              |
|  ClassName   |
|              |
|______________|

---
Debug TortoiseSVN
Create registry DWORD value
HKEY Current User\Software\TortoiseSVN\Debug
set its value = 1

Excel diff
(http://stackoverflow.com/questions/114698/how-do-i-create-a-proper-diff-of-two-spreadsheets-when-i-perform-a-version-contr)
https://github.com/na-ka-na/ExcelCompare
---

---
errata for the PIL
Programming in Lua, error pages.(http://www.lua.org/pil/errata.html#3ed)
Variadic Function, 可变参函数
a={}
a["x"] = 10
print(a["x"]) --> 10
print(a["y"]) --> nil
Table fields evaluate to nil when not initialized.
To represent a conventional array or a list, you simply use a table with integer keys.
It is customary in Lua to start arrays with one and several facilities in Lua stick to this convention.
Often, the length is implicit. Remember that any non-initialized index results in nil. sentinel.
A list without nil elements inside it is a sequence.
For sequences, Lua offer '#', it returns the last index, or the length of the sequence.
http://www.lua.org/manual/5.2/manual.html#3.4.6
Unless a __len metamethod is given, the length of a table t is only defined if the table is a sequence, that is, the set of its positive numeric keys is equal to {1..n} for some non-negative integer n.In that case, n is its length.
Note that a table like
     {10, 20, nil, 40}
is not a sequence, because it has the key 4 but does not have the key 3.
Note, however, that non-numeric keys do not interfere with whether a table is a sequence.

table.unpack receives an array and returns as results all elements from the array, starting from index 1.
unpack uses the length operator to know how mant elements to return. So it works  on proper sequences.

table.pack receives any number of arguments and returns a new table with all its arguments, just like {...}
But this table has also an extra field "n", with the total number of args.

Chapter 6
A closure is a function plus all it needs to access non-local variable directly.
What is a value in Lua is the closure, not the function.

o:foo(x) <--> o.foo(o, x)

Chapter 7
Generic for, Step 1:
The first thing the for does is to evaluate the expressions after the keyword <<in>>.
These expressions should result in the three values kept by the for :
the iterator function, 
the invariant state, and 
the initial value for the control variable.
Step 2:
calls the iterator function with two arguments : the invariant state and the control variable.
Step 3:
assigns the values returned by the iterator function to the vars decalred by its variable list.
Step 4:
If the first value returned is nil, the loop terminates. Or, the for executes its body and call the iteration function again.

for var1, var2, ..., varn in <explist> do <block> end

do
	local _f, _s, _var = <explist>
	while true do
		local var1, var2,...,varn = _f(_s, _var)
		_var = var1
		if _var == nil then break end
		<block>
	end
end

Chapter 8 -- Compilation Execution and Erros
The distinguishing feature of interpreted languages is that it's possible(and easy) to execute code generated on the fly.
-- 8.1 Compilation --
dofile() run chunks of Lua code.
loadfile() loads a Lua chunk from file, but it does not run the chunk. It compiles the chunk and return the compiled chunk as a func.
loadfile() does not raise errors. 
a,b = loadfile('error_file.lua')
In case of error, a is nil, b is error message(Not necessarily a string, maybe a table).

load() is similar to loadfile() except that it reads its chunk from a string.
assert(load(str))() can generate clear error messages
load() does not compile with lexical scoping. load() always compiles its chunk in the global environment.
load() calls the reader func successively until it returns nil.
f = load(io.lines(filename, "*L"))
f = load(io.lines(filename, 1024)) is more efficient.

f = load("i=i+1")
f = function () i = i + 1 end
The seoncd line is much faster, because Lua compiles the func together with its enclosing chunk.
The first line load() involves a separate compilation.

Lua treats any independent chunk as the body of an anonymous func with a variable number of args.
load("a=1")
function(...) a = 1 end
A common mistake is to assume that loading a chunk deifines functions.
In Lua, function definitions are assignments, they happen at runtime, not compile time.
-- file 'foo.lua'
function foo(x)
  print(x)
end

f = loadfile('foo.lua')
foo is compiled but not defined yet.
print(foo) --> nil
f()        --> defines foo()
foo("ok")  --> "ok"

-- 8.2 Precompiled code --
How to produce a binary chunk ? luac -o prog.lc prog.lua
compile lua to bytecode : luac -l test.lua
loadfile() and load() accept precompiled code.
string.dump() receives a Lua function and returns its precompiled code as a string.
Google "lua opcode". Gain some knowledge about precompiled form Lua code.

--8.3 C code --
local path = "/usr/local/lib/lua/5.1/socket.so"
local f = package.loadlib(path, "luaopen_socket")
loadlib() does not call the given function. It returns the C function as a Lua function.
On error, it returns nil + error message.
More often than not, we load C libraries using require().

-- 8.4 Errors --
Errare humanum est.(to err is human)
Whenever an error occurs, Lua ends the current chunk and returns to the application.
Explicitly raise an error calling the error("You are error!!")
assert(io.read("*n"), "invalid input")
An exception that is easily avoided should raise an error, otherwise it should return an error code.
file = assert(io.open("file_name", "r"))

-- 8.5 Error handling and Exceptions --
If you need to handle errors in Lua, you must use the pcall() function to encapsulate your code.
pcall() calls its first arg in protected mode, so that it catches any errors while the function is running.
If no errors, return true + any values returned by the call.
Otherwise, return false + error message.
The error message does not have to be a string, pcall() will return any Lua value that you passed to error() function.

-- 8.6 Erorr messages and Tracebacks --
Whenever the error message is a string, Lua tries to add some info(file name, and line number) about the error happened.
xcall(), debug.debug, debug.traceback

-- Chapter 9, Coroutine --
-- 9.1 Coroutine basics --
Coroutines, on the other hand, are collaborative:
At any given time, a program with coroutines is running only one of its coroutines,
and this running coroutine suspends its execution only when it explicitly requests to be suspended.

A coroutine can be in one of four states : suspended, running, dead, and normal.
The real power of coroutines steams from the yield function, 
which allows a running coroutine to suspend its own execution so that it can be resumed later.

coroutine.resume() runs in protected mode. If there is any error inside a coroutine, 
Lua will not show  the error message, but instead return it to the resume call.

Pair resume-yield can exchange data.
yield() returns any extra arguments passed to the corresponding resume().
When a coroutine ends, any value returned by its main function go to the corresponding resume().

-- 9.2 Pipes and Filters --

get benchmark
os.clock()

How to list all functions in lua file?
(http://stackoverflow.com/questions/17294323/list-lua-functions-in-a-file)
luac -o temp script.lua

Chapter 25 the C API
This ability to be used as a library to extend an application is what makes Lua an extension language.
At the same time, a program that uses Lua can register new functions in the Lua environment; 
such functions are implemented in C (or another language), so that they can add facilities that cannot be written directly in Lua. 
This is what makes Lua an extensible language.

In the first kind, C has the control and Lua is the library. The C code in this kind of interaction is what we call application code. 
In the second kind, Lua has the control and C is the library. Here, the C code is called library code.

A major component in the communication between Lua and C is an omnipresent virtual stack.
Everything defined in lua.h has a lua_ prefix.

===
Install luarocks
wget http://keplerproject.github.io/luarocks/releases/luarocks-2.2.2.tar.gz
tar -zxf luarocks-2.2.2.tar.gz
cd luarocks-2.2.2/
export PATH=/home/giant/bin/lua/include:/home/giant/bin/lua/bin:$PATH
./configure --prefix=/home/giant/bin/lua
make build
make install
===
Install luasocket using luarocks:
sudo apt-get update
sudo apt-get install unzip
luarocks install luasocket-3.0rc1-2.src.rock

lua
> require("socket")
table:0x12345678

export LUA_PATH="/home/giant/bin/lua/share/lua/5.3/?.lua;?.lua"
export LUA_CPATH="/home/giant/bin/lua/lib/lua/5.3/?.so;?.so"
---
Print lua stack 
print(debug.traceback())
---
lua 5.1 -> 5.2 error
LUA_HOOKTAILRET  --> LUA_HOOKTAILCALL 
lua_getfenv --> lua_getenv
lua_objlen --> lua_rawlen 

How to compile dynamic lua library file?
(http://stackoverflow.com/questions/20848275/compiling-lua-create-so-files)
vim src/Makefile
CFLAGS= -O2 -Wall -DLUA_COMPAT_ALL $(SYSCFLAGS) $(MYCFLAGS) -fPIC
LUA_A = liblua.so

Where to download lua binary executables and libraries?
http://luabinaries.sourceforge.net/

Lua syntax:
1. The __index Metamethod
I said earlier that, when we access an absent field in a table, the result is nil. This is true, but it is not the whole truth. Actually, such access triggers the interpreter to look for an __index metamethod: If there is no such method, as usually happens, then the access results in nil; otherwise, the metamethod will provide the result.

    -- create a namespace
    Window = {}
    -- create the prototype with default values
    Window.prototype = {x=0, y=0, width=100, height=100, }
    -- create a metatable
    Window.mt = {}
    -- declare the constructor function
    function Window.new (o)
      setmetatable(o, Window.mt)
      return o
    end
    
    Window.mt.__index = function (table, key)
      return Window.prototype[key]
    end
    
    w = Window.new{x=10, y=20}
    print(w.width)    --> 100
    
    When it is a table, Lua redoes the access in that table. Therefore, in our previous example, we could declare __index simply as :
    Window.mt.__index = Window.prototype
    
    The __newindex metamethod does for table updates what __index does for table accesses. When you assign a value to an absent index in a table, the interpreter looks for a __newindex metamethod: If there is one, the interpreter calls it instead of making the assignment. Like __index, if the metamethod is a table, the interpreter does the assignment in that table, instead of in the original one. Moreover, there is a raw function that allows you to bypass the metamethod: The call rawset(t, k, v) sets the value v in key k of table t without invoking any metamethod.

    The combined use of __index and __newindex metamethods allows several powerful constructs in Lua, from read-only tables to tables with default values to inheritance for object-oriented programming. In the rest of this chapter we see some of these uses. Object-oriented programming has its own chapter.
    
2. ipairs and pairs difference
(http://www.luafaq.org/#T1.10)
ipairs(t)
Returns three values (an iterator function, the table t, and 0) so that the construction
     for i,v in ipairs(t) do body end
will iterate over the key–value pairs (1,t[1]), (2,t[2]), ..., up to the first nil value.

pairs(t)
If t has a metamethod __pairs, calls it with t as argument and returns the first three results from the call.
Otherwise, returns three values: the next function, the table t, and nil, so that the construction
     for k,v in pairs(t) do body end
will iterate over all key–value pairs of table t.

next (table [, index])
Allows a program to traverse all fields of a table. Its first argument is a table and its second argument is an index in this table. next returns the next index of the table and its associated value. When called with nil as its second argument, next returns an initial index and its associated value. When called with the last index, or with nil in an empty table, next returns nil. If the second argument is absent, then it is interpreted as nil. In particular, you can use next(t) to check whether a table is empty.

The order in which the indices are enumerated is not specified, even for numeric indices. (To traverse a table in numerical order, use a numerical for.)

The behavior of next is undefined if, during the traversal, you assign any value to a non-existent field in the table. You may however modify existing fields. In particular, you may clear existing fields.

3.How to copy table in lua?(http://lua-users.org/wiki/CopyTable)
function deepcopy(orig)
    local orig_type = type(orig)
    local copy
    if orig_type == 'table' then
        copy = {}
        for orig_key, orig_value in next, orig, nil do
            copy[deepcopy(orig_key)] = deepcopy(orig_value)
        end
        setmetatable(copy, deepcopy(getmetatable(orig)))
    else -- number, string, boolean, etc
        copy = orig
    end
    return copy
end

---
How to run makefile in windows ? makefile make in windows?
open VS2013 Native Command Prompt
nmake /f makefile.win
---
gnu makefile special variables:
(https://www.gnu.org/software/make/manual/html_node/Automatic-Variables.html)
$@
The file name of the target of the rule. If the target is an archive member, then ‘$@’ is the name of the archive file. In a pattern rule that has multiple targets (see Introduction to Pattern Rules), ‘$@’ is the name of whichever target caused the rule’s recipe to be run.
$?
The names of all the prerequisites that are newer than the target, with spaces between them. For prerequisites which are archive members, only the named member is used (see Archives).
---
Compilation fails with “relocation R_X86_64_32 against `.rodata.str1.8' can not be used when making a shared object”
(http://stackoverflow.com/questions/19364969/compilation-fails-with-relocation-r-x86-64-32-against-rodata-str1-8-can-not)
Do what the compiler tells you to do, i.e. recompile with -fPIC. To learn what does this flag do and why you need it in this case, see Code Generation Options of the GCC manual.
---
Github follow an organization
(Following an organization wouldn't get you anything since they don't do anything. (People who belong to organizations do things).)
$.post('https://github.com/users/follow?target=org_name')
---

Show a path:
C:>path
C:>echo %path%
C:>set path